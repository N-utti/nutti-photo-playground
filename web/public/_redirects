# Cloudflare Pages SPA fallback.
#
# 이 앱의 라우팅은 전부 클라이언트(react-router)입니다. 정적 호스팅은 /styles/101 이나
# /jobs/{id} 같은 경로에서 그 이름의 파일을 찾다가 404 를 내므로, 모든 경로를
# index.html 로 넘겨 라우터가 처리하게 합니다.
#
# 특히 /auth/callback/{provider} 는 카카오·네이버·카페24가 **직접 보내는** 주소라
# 우회로가 없습니다 — 이 줄이 없으면 로그인이 배포 직후 100% 깨집니다.
#
# 200(rewrite)이지 302 가 아닙니다. 리다이렉트로 처리하면 주소창이 / 로 바뀌면서
# OAuth 콜백의 code·state 쿼리가 사라집니다.
/*    /index.html   200
