VITE_ENABLE_MOCKS=false

# TODO(미확정): API 서버 도메인이 아직 정해지지 않았습니다.
# docs 는 놀이터 프론트를 `play.nutti.co.kr` 형태로만 언급하고(README "기술 제약",
# 06 §11) API 쪽 호스트는 어디에도 없습니다. 아래 값은 자리표시자이며 배포 전에
# 실제 호스트로 바꿔야 합니다. 프론트와 오리진이 다르므로 CORS 가 필요합니다(이슈 #3).
VITE_API_BASE_URL=https://api.example.invalid/v1
