/**
 * 커스텀 프롬프트(W-08 「직접 만들기」) 비용의 **추정치**.
 *
 * 서버가 아니라 프론트가 이 값을 갖고 있다는 게 문제라 한 곳에 모아 둡니다. 진짜
 * 값은 `app_setting.custom_prompt_credit_cost` 이고, 그 행이 없을 때만 서버가 2 로
 * 떨어집니다(`app/routers/jobs.py`). 프리셋 비용은 `GET /v1/styles` 의 `credit_cost`
 * 로 오는데 커스텀 비용은 **읽을 경로가 §3 에 없습니다** — 노출을 요청한 게 이슈
 * #149 이고, 오면 이 파일을 지웁니다.
 *
 * 모아 둔 이유가 «중복 제거» 만은 아닙니다. 이 숫자는 세 화면에 **문장 안에** 박혀
 * 있었습니다 — W-02 카탈로그 하단 링크 · W-04 업로드 후 링크 · W-08 만들기 버튼.
 * 흩어진 리터럴은 #149 가 착지하는 날 한두 곳만 고쳐지고, 남은 화면이 «2 크레딧»
 * 이라고 말하며 3 을 빼 갑니다. 값의 출처가 하나면 그날 지울 자리도 하나입니다.
 *
 * W-08 은 402 응답의 `detail.required` 로 서버의 실제 값을 배우면 그 값을 씁니다.
 * 다른 두 화면은 아직 요청을 보내기 전이라 배울 방법이 없습니다 — 그 절반은 #149
 * 가 와야 닫힙니다.
 */
export const CUSTOM_PROMPT_COST_ESTIMATE = 2
